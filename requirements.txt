# sanic finally did a timely update recently, so we can live
# off the release instead of the commit.
sanic==19.6.2
sanic-cors==0.9.8

asyncpg==0.24.0
bcrypt==3.2.0
itsdangerous==1.1.0

cerberus==1.3.4

# going back to non-git version since
# the py3.7 fixes went to 1.2
# 2020-06-04: bumped to 1.3.1 for python3.8 compat
aioredis==1.3.1

# The worst idea ever is to add aiohttp to a project.
# and I did it.
# fuck aiohttp.
aiohttp==3.5.4

# May god have mercy on my soul
Pillow==8.3.1

# testing stuff
pytest==6.2.4
pytest-cov==2.12.1
pyflakes==2.3.1

pytest-sanic==1.0.0

aioinflux==0.9.0

cryptography==2.7

# You'll need libmagic for this.
# On linux, use your package manager to install libmagic.
# On win/mac, see README here:
# https://github.com/ahupp/python-magic
python-magic==0.4.24

parsedatetime==2.6

# for email checking
dnspython==1.16.0
